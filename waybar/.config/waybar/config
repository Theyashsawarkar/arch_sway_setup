{
	"layer": "top",
	"position": "top",
	"height": 32,
	"spacing": 5,

	"modules-center": ["clock"],
	"modules-left": ["sway/workspaces", "keyboard-state", "custom/docker", "custom/caffeine", "bluetooth"],

	"modules-right": [
		"network", 
		"network#speed",
		"backlight",
		"pulseaudio",
		"battery"
	],

	"sway/workspaces": {
		"disable-scroll": true,
		"all-outputs": true,
		"format": "{name}",
		"persistent-workspaces": {
			"1": [],
			"2": [],
			"3": []
		}
	},

	"backlight": {
		"format": "  {percent}%",
		"tooltip": false,
		"on-scroll-up": "brightnessctl set +5%",
		"on-scroll-down": "brightnessctl set 5%-"
	},

	"keyboard-state": {
		"capslock": true,
		"numlock": true,
		"format": "{name} {icon}",
		"format-icons": {
			"locked": "",
			"unlocked": ""
		}
	},

	"clock": {
	// Formats as: Wed, August 12   02:11 PM
	"format": "  {:%a, %B %d   %I:%M %p}",
	"tooltip-format": "<tt><small>{calendar}</small></tt>"
	},

	"custom/docker": {
		"exec": "docker ps -q | wc -l",
		"interval": 5,
		"format": "  {}",
		"tooltip": false
	},

	"network": {
		"format-wifi": "  {essid}",
		"format-ethernet": "  {ipaddr}",
		"format-disconnected": "  Offline",
		"interval": 2,
		"max-length": 30
	},

	"network#speed": {
		"interval": 2,
		"format": "⬇ {bandwidthDownBytes}  ⬆ {bandwidthUpBytes}",
		"tooltip": false
	},

	"bluetooth": {
		"format-off": "",
		"format-on": "",
		"format-connected": "  {device_alias}",
		"format-connected-battery": "  {device_alias}  {device_battery_percentage}%",
		"tooltip-format": "Bluetooth Status: {status}",
		"on-click": "blueman-manager",
		"max-length": 25
	},

	"custom/caffeine": {
		"exec": "~/.local/bin/caffeine-status.sh",
		"on-click": "~/.local/bin/caffeine-toggle.sh",
		"interval": 5,
		"return-type": "json"
	},

	"battery": {
		"states": {
			"warning": 30,
			"critical": 15
		},
		"format": "{icon}  {capacity}%",
		"format-charging": "  {capacity}%",
		"format-plugged": "  {capacity}%",
		"format-icons": ["", "", "", "", ""]
	},

	"pulseaudio": {
		"format": "{icon}  {volume}%",
		"format-muted": "  Muted",
		"format-icons": {
			"default": ["", "", ""]
		},
		"on-click": "pavucontrol"
	}
}
